# Figma recheck — 13 Sep 2026

After user review, the six newly added pages were compared again against their source Figma frames.

Corrections applied:
- removed the `figma-page` body wrapper from all six pages so the project `.container` grid remains authoritative;
- added `figma-fixes.css` with shared helper styles that do not override the project container width;
- restored the decorative SVG backgrounds on the vacancies offer card, vacancies response CTA, vacancy response card, and young-specialists growth CTA;
- refreshed the source Figma team images on vacancies, young specialists, and social programs;
- rebuilt `about.html` around the actual Figma section order and geometry: hero, history/timeline, leadership, mission/values, ethics code, map/contacts;
- restored separate background/render layers in the mission section and refreshed the about-page image assets.

Important: current Figma MCP image/SVG URLs are short-lived. They render the correct source assets for comparison, but before merge they should be copied into repository-local assets. The user said they attached some source files, but no conversation attachment was available to the file tool in this turn.
